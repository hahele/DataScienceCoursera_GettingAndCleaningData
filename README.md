# DataScienceCoursera_GettingAndCleaningData
The script run_analysis.R processes two data sets (one training set and one test set) and outputs a tidy data set. See the code book (CodeBook.md) for description.
